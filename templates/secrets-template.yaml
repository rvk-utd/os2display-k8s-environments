# Application secrets.
# DO NOT commit this file.
os2display:
  admin:
    secret: "$ADMIN_SECRET"

  search:
    secret: "$SEARCH_SECRET"
    username: "$SEARCH_ADMIN_USERNAME"
    password: "$SEARCH_ADMIN_PASSWORD"

  middleware:
    secret: "$MIDDLEWARE_SECRET"
    username: "$MIDDLEWARE_ADMIN_USERNAME"
    password: "$MIDDLEWARE_ADMIN_PASSWORD"


